# personal access token with read:user scope — used by the CLI when not in --demo mode.
# copy to .env (gitignored) or export it in your shell. never commit a real token.
GITHUB_TOKEN=ghp_your_token_here
